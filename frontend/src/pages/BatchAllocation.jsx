import React from 'react';
import Layout from '../components/Layout';
import BatchAllocationContent from '../components/Admissions/BatchAllocationContent';

const BatchAllocation = () => {
    return (
        <Layout activePage="Admissions & Sales">
            <BatchAllocationContent />
        </Layout>
    );
};

export default BatchAllocation;
