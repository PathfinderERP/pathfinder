import React from 'react';
import Layout from '../components/Layout';
import SectionAllotmentContent from '../components/Admissions/SectionAllotmentContent';

const SectionAllotment = () => {
    return (
        <Layout activePage="Admissions & Sales">
            <SectionAllotmentContent />
        </Layout>
    );
};

export default SectionAllotment;
